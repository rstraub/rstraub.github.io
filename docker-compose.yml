version: '3'

services:
    jekyll:
        image: jekyll/jekyll:latest
        command: jekyll serve --force_polling --livereload 
        environment:
            - JEKYLL_ENV=docker
        ports:
            - 4000:4000
            - 35729:35729
        volumes:
            - .:/srv/jekyll